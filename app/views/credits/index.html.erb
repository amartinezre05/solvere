<% content_for :title, "Mis créditos" %>

<%= tag.div(flash[:alert], style: "color:red") if flash[:alert] %>
<%= tag.div(flash[:notice], style: "color:green") if flash[:notice] %>

<header>
  <span><%= Current.user.email_address %></span>
  <%= button_to "Cerrar sesión", session_path, method: :delete %>
</header>

<h1>Mis créditos</h1>

<p><%= link_to "Nuevo crédito", new_credit_path %></p>

<% if @credits.any? %>
  <table>
    <thead>
      <tr>
        <th>Tipo</th>
        <th>Entidad</th>
        <th>Saldo actual</th>
        <th>Próxima cuota</th>
        <th>Estado</th>
      </tr>
    </thead>
    <tbody>
      <% @credits.each do |credit| %>
        <% schedule = @schedules[credit] %>
        <% next_installment = schedule.next_installment %>
        <tr>
          <td><%= credit.credit_type.name %></td>
          <td><%= link_to credit.lender_name, credit_path(credit) %></td>
          <td><%= format_cop(schedule.current_balance) %></td>
          <td>
            <% if next_installment %>
              <%= next_installment.date.strftime("%d/%m/%Y") %> — <%= format_cop(next_installment.payment) %>
            <% else %>
              Sin cuotas pendientes
            <% end %>
          </td>
          <td><%= credit.status.humanize %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>Todavía no tienes créditos registrados.</p>
<% end %>
